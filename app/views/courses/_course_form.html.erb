<%= form_for @course do |f| %>
  <%= f.label :title %></br>
  <%= f.text_field :title %>
  </br>
  <%= f.label :body %></br>
  <%= f.cktext_area :body %>
  <%= f.submit 'Create', class: 'btn-primary' %>
<% end %>